*&---------------------------------------------------------------------*
*& Report ZTMP
*&---------------------------------------------------------------------*
*& 功能说明：
*&   从表 ztint_market_req 查询数据并以 ALV 报表展示。
*&   采用 INCLUDE 模块化：ZTMP_TOP（数据/选择屏幕）、ZTMP_F01（子程序）。
*&---------------------------------------------------------------------*
*& 包含程序：
*&   ZTMP_TOP  - 数据定义、选择屏幕参数
*&   ZTMP_F01  - 子程序：初始化、取数、ALV 展示
*&---------------------------------------------------------------------*
REPORT ztmp MESSAGE-ID 00.

* 数据定义与选择屏幕（参数、选择块等）
INCLUDE ztmp_top.

* 子程序：取数、ALV 展示（get_data、display_report 等）
INCLUDE ztmp_f01.

*&---------------------------------------------------------------------*
*& INITIALIZATION - 初始化
*&---------------------------------------------------------------------*
*& 说明：程序加载时执行；设置选择屏幕标题，并调用 init_defaults 初始化默认日期等
*&---------------------------------------------------------------------*
INITIALIZATION.
  tit = '选择条件'.           " 选择屏幕块标题
  PERFORM init_defaults.      " 初始化默认日期等

*&---------------------------------------------------------------------*
*& START-OF-SELECTION - 主逻辑
*&---------------------------------------------------------------------*
*& 说明：用户执行报表后触发；先按选择条件取数，再以 ALV 网格展示
*&---------------------------------------------------------------------*
START-OF-SELECTION.
  PERFORM get_data.           " 从 ztint_market_req 等取数
  PERFORM display_report.     " ALV 展示
